<p><strong>Release v1.11a:</strong></p>
<p>Added:</p>
<ul>
<li>API-Bump to API 14</li>
<li>GradientColors: Added a Preview</li>
<li>GroupHandler: Added a Dropdown to sort the Groupings-UI</li>
</ul>
<p>Fixes:</p>
<ul>
<li>SaveHandler: On Startup the Addon always loaded the Profile associated with the first Spec.</li>
</ul>
<p><strong>Release v1.11:</strong></p>
<p>Added:</p>
<ul>
<li>Bars: Now have orientations. (Left-&gt;Right, Top-&gt;Bottom etc.)</li>
<li>RaidMarkers: Now supports a color aswell.</li>
<li>FastMenu: Now has a 'Master Loot' Button to open the Master-Loot Bag.</li>
<li>Paths: Added Icons and a Color for paths.</li>
<li>Positioning: You can now choose, which corner the Anchor defines.</li>
<li>Profile-Independant Settings: Added Settings, which are completely Character dependant: Profile-Selection and Groupings (Both Settings will be reset with this Update - The Profile itself will not be lost)</li>
<li>Profiles:&nbsp;Can now be&nbsp;applied to different Actionsets.</li>
</ul>
<p>Fixes:</p>
<ul>
<li>Gradient Colors: Some Updating-Problems within the Settings.</li>
</ul>
<p><strong>Release v1.10a:</strong></p>
<p>Added:</p>
<ul>
<li>TestMode: All test-units now fake to be positioned where the player engaged test-mode.</li>
<li>The three Handlers are now linked to the Main Addon. This means the Addon-Tab (Esc) is now properly populated - From now on Errors will most likely appear directly on your screen. Report them please.</li>
</ul>
<p>Fixes:</p>
<ul>
<li>Consumables: Fixed an (probably unnoticed) error when coming out of loadingscreen.</li>
<li>GroupHandler: Changing a Groups Name now properly updates your own frames.</li>
<li>Updates: The frequent Updates now work properly (WAY less Updates - you might want to retune this)</li>
<li>UnitHandler: Fixed a potential Error, whenever your&nbsp;group gets smaller</li>
</ul>
<p><strong>Release v1.10:</strong></p>
<p>Added:</p>
<ul>
<li>A proper Test-Mode! (You may want to deactivate the distance-fader to test)</li>
<li>Grouping sharing spam reduced to once all 3 seconds. (does not reduce&nbsp;resharing)</li>
</ul>
<p>Fixes:</p>
<ul>
<li>The Addon could end up looping with another Player to publish their groupings back and forth.</li>
</ul>
<p><strong>Release v1.9c:</strong></p>
<p>Added:</p>
<ul>
<li>UnitHandler now (additionally) uses 'UnitCreated' to create Unit-Updates (This should SIGNIFICANTLY decrease the time for the first update of a Unit)</li>
</ul>
<p>Fixes:</p>
<ul>
<li>Distance Fader: Fixed an Error, which could occur after leaving a Loading-Screen</li>
</ul>
<p><strong>Release v1.9b:</strong></p>
<p>Added:</p>
<ul>
<li>Leave Button for the FastMenu.</li>
</ul>
<p>Fixes:</p>
<ul>
<li>Make Group-Headers be sized to full width. Most recent Update made all frames 2px greater in each direction, except the headers.</li>
<li>Updated the Defaults to be spaced&nbsp;as they were before.</li>
</ul>
<p>&nbsp;</p>
<p><strong>Release v1.9a:</strong></p>
<p>FastMenu:</p>
<ul>
<li>Reworked the Menu to only show useful buttons (informative or clickable)</li>
<li>Added 'Raid Manager' Button to open up the Default Leader-Options (RaidFrameLeaderOptions Addon needs to be activated)</li>
</ul>
<p><strong>Release v1.9:</strong></p>
<p>Added:</p>
<ul>
<li>Colorpicker &nbsp;- Added&nbsp;'2x'-option to intensify any&nbsp;color to their double value.</li>
<li>Bars - Are now able to be layered&nbsp;by custom.</li>
<li>General - Settings now has a button to show an anchor for dragging the frames around (top-left)</li>
<li>DC-Icon - Added a Disconnect Icon to be shown for all disconnected Players.</li>
</ul>
<p>Fixes:</p>
<ul>
<li>FastMenu - Retexted 'Tank' and 'DPS' buttons, scince tank was&nbsp;something really weird.</li>
<li>FastMenu -&nbsp;Changing loot threshold&nbsp;didn't change anything.</li>
</ul>
<p>&nbsp;</p>
<p><strong>Release v1.8:</strong></p>
<p>Added:</p>
<ul>
<li>API Version was changed to 13</li>
<li>Role Icon - new Module to display role icons.</li>
<li>Reworked the loadingprocess of the Settings. Tabs now get loaded on first view.</li>
<li>Pedantic Updater - frequently updates 'backupdata' for the unit, to have even those out of range to have some updated data.</li>
<li>Heal Absorb Bar&amp;Text - With API 13 Carbine added some Bars to their Addons, this is one.</li>
<li>Health Clamp Bars - With API 13 Carbine added some Bars to their Addons, these are two more.</li>
<li>Debuffs - Well, the same as Buffs, only with 'De'</li>
<li>Bars - Bars are now Layerd properly, so that all those in 'Stacking'-Mode are always behind those in 'Offset'-Mode and these are always behind those in 'Fixed'-Mode. This affects the Texts applied to these Bars aswell.</li>
<li>Colorbuttons / Colorpicker - all of these do now have four shades of Gray behind the actual colors to make colorpicking easier.</li>
</ul>
<p>Fixes:</p>
<ul>
<li>Mouse Interaction - Tried to fix a problem with reapplying old targets. Need more input on this feature.</li>
<li>Errors while Switching Profiles.</li>
<li>Constant Colors - Reworked Settings to properly&nbsp;update when switching Profiles.</li>
</ul>
<p><strong>Release v1.7:</strong></p>
<p>Added:</p>
<ul>
<li>Bars - You now have the ability to choose the Textposition.</li>
<li>Bar: Spacer - a Bar with a static progress, mainly for placing text anywhere on the frame.</li>
<li>More Defaults&nbsp;-&nbsp;'Mischh'(the previous default), 'VinceRaidFrames' and 'Grid' - bth new try to come close to the defaults of their original.</li>
<li>Importing / Exporting Profiles&nbsp;-&nbsp;Note: The Profile-Button moved into a Tab.</li>
<li>Grouping - You can now choose to accept from all assistants.</li>
<li>Grouping - The Raidleader can now choose to reshare/republish accepted settings to the group.</li>
</ul>
<p>Fixes:</p>
<ul>
<li>Previewing Top and Bottom Header Space didnt work</li>
<li>Some Errors when Switching Profiles</li>
<li>Moving a bar in 'Stacking'-Mode more than one position broke the whole frameTemplate</li>
</ul>
<p><strong>Release v1.6b:</strong></p>
<p>Added:</p>
<ul>
<li>Grouping - Rearranged UI to add more Buttons. MORE BUTTONS!
<ul>
<li>Button 'Publish in Chat' readded to publish your grouping a single time through chat</li>
<li>Button 'Output Readable' added to output a readable version of your groups</li>
</ul>
</li>
</ul>
<p>Fixes:</p>
<ul>
<li>Published Groups from chat are now read correctly again. (Bugged scince 1.6a)</li>
<li>The GroupingFrom had to be loaded to import other players groups. (Bugged scince always)</li>
</ul>
<p><strong>Release v1.6a:</strong></p>
<p>Added:</p>
<ul>
<li>Buffs - You can now add Colored Texts to the Icons.</li>
<li>Grouping&nbsp;- Now uses ICComm istead of Chat.</li>
</ul>
<p><strong>Release v1.6:</strong></p>
<p>Added:</p>
<ul>
<li>Buffs - You can choose to show a colored box, if there is a specified buff active.</li>
<li>Arrow Icon&nbsp;-&nbsp;Targeting the unit with a colored arrow, if far enough away.</li>
</ul>
<p>Fixes:</p>
<ul>
<li>Previews - Error with 'Not Shown' bars.</li>
<li>Actually load bar-textures (non-ForgeUI-Users were not able to use them)</li>
<li>Consumables - Hide all Icons, if no information about buffs available</li>
</ul>
<p><strong>Release v1.5b:</strong></p>
<p>Added:</p>
<ul>
<li>FastMenu - Join and Referral Settings.</li>
<li>MouseInteraction - Choice to Invert on Mouse leaving</li>
<li>MouseInteraction - Advanced Mouseover Detection</li>
<li>Updates - Debug button</li>
</ul>
<p><strong>Release v1.5a:</strong></p>
<p>Added:</p>
<ul>
<li>All Sliders do now have a Textbox to their left, instead of a Label.</li>
</ul>
<p><strong>Release v1.5:</strong></p>
<p>Added:</p>
<ul>
<li>Icons are now sized a little different.</li>
<li>FrameHandler: Change Background Color</li>
<li>Bars: Changeable Bar-Textures</li>
<li>You can now Copy from the Profile 'Default', which copies from Defaults.</li>
<li>Interrupt Indicator Module (Misc)</li>
<li>Some more stuff the SaveHandler will now check after loading a frame template</li>
</ul>
<p>Fixes:</p>
<ul>
<li>A Error which sometimes occured on switching Profiles (call function 'Get', a boolean)</li>
<li>Possible empty buttons within Ungrouped Tab of&nbsp;Grouping-Frame</li>
</ul>
<p><strong>Release v1.4:</strong></p>
<p>Added:</p>
<ul>
<li>ConsumableIcon: Show Icons for Consumbales used by your Raidmembers. (Food, Speedflask, Boost, Firebuff)</li>
<li>Split the FrameHandler into Size, Position &amp; Spaces.</li>
<li>Previews for Bars, Icons and FrameHandler.</li>
<li>German Localization:&nbsp;Colors.</li>
<li>German Localization:&nbsp;Texts.</li>
<li>German Localization:&nbsp;Misc.</li>
</ul>
<p>Fixes:</p>
<ul>
<li>Some Minor, probably unnoticed Problems.</li>
</ul>
<p><strong>Release v1.3:</strong></p>
<p>Fixes:</p>
<ul>
<li>Checkbox in Update-Tab didn't do anything.</li>
<li>Icons now have a&nbsp;Position in any Situation.</li>
</ul>
<p>Added:</p>
<ul>
<li>German Localization: Icons</li>
<li>German Localization: Bars</li>
<li>Readycheck-Icon: Show for a specified period of time, who has accepted/declined a Readycheck.</li>
</ul>
<p><strong>Release v1.2d:</strong></p>
<p>Fixes:</p>
<ul>
<li>Newly introduced Error (A Update removed 'MachtingGame'. Removed requirement.)</li>
</ul>
<p><strong>Release v1.2c:</strong></p>
<p>Added:</p>
<ul>
<li>FastMenu: New Button to change to your groups Instance (not Dungeons, Instanced Regions like being in different Instances of Thayd)</li>
<li>Grouping: Options to imitate VinceRaidFrames in custom-group activation: (both are optional)
<ul>
<li>Activate grouping, once accepted a grouping from chat.</li>
<li>Deactivate grouping, whenever joining a group.</li>
</ul>
</li>
</ul>
<p><strong>Release v1.2b:</strong></p>
<p>Fixes:</p>
<ul>
<li>The Grouping-Frame should now work again, after being broken in v1.2a, sorry.</li>
</ul>
<p><strong>Release v1.2a:</strong></p>
<p>Fixes:</p>
<ul>
<li>Within Raided Instances (aka Battlegrounds) you have not been able to change Role (untested)</li>
</ul>
<p>Added:</p>
<ul>
<li>GroupHandler: Allow to set a sort method to be used&nbsp;on each group individually</li>
<li>Localization: started to Localize the Addon to German.</li>
</ul>
<p><strong>Release v1.2:</strong></p>
<p>Fixes:</p>
<ul>
<li>Icons - You werent able to move Icons up/down</li>
<li>FastMenu - From now on, within a Group, only the Leader can Readycheck</li>
<li>SaveHandler -&nbsp;after loading profiles the Addon will check for broken&nbsp;stuff within the frame template and report them.</li>
</ul>
<p>Added:</p>
<ul>
<li>LeadMarker - Possibility to use&nbsp;a filled color rather than the default icon.</li>
<li>MouseInteraction - Added 'Show Hint Arrow'-Option to the Buttons.</li>
<li>NameModule - Added Nicknames.</li>
<li>GroupHandler - Instead of using the Default tank-heal-dps Groups you can now use the Grouping-Frame to create your own groups and share them with your party. This sharing is compatible with VinceRaidFrames.</li>
</ul>
<p><strong>Release v1.1:</strong></p>
<ul>
<li>Added Module: Dispel Indicator&nbsp;<img src="http://i.imgur.com/dpj1oKe.png" alt="Dispel Indicator" width="250" height="23" /></li>
<li>Added Button into&nbsp;FastMenu to open Settings</li>
<li>Added Button into Settings (top-left) to add yourself to your (possible non-existant group) - for testing your Settings.</li>
<li>Recolored FastMenu to match new Button Colors.</li>
</ul>